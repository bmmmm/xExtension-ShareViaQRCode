# Supply-chain hardening (applies even to this single-package repo).
# See ~/ops/reference/pnpm-supply-chain.md for the rationale.
minimumReleaseAge: 4320   # 3-day cooldown — freshly compromised releases are usually yanked within hours
onlyBuiltDependencies: [] # allowlist for build/postinstall scripts; empty = none may run
